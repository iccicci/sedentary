version: "2"
checks:
  file-lines:
    config:
      threshold: 500
  identical-code:
    config:
      threshold: 100
  method-complexity:
    config:
      threshold: 100
  method-lines:
    config:
      threshold: 200
  similar-code:
    config:
      threshold: 100
plugins:
  eslint:
    enabled: true
exclude_patterns:
  - "**/node_modules/"
  - "**/*.d.ts"
