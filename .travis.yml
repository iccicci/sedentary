env:
  global:
    - CODECLIMATE_REPO_TOKEN=1aa1f737e7bf7d2859a2c7d9a0d9634a0d9aa89e3a19476d576faa7d02a1d46f

sudo: false

language: node_js

node_js:
  - "12"
  - "10"

after_script:
  - if [[ `node --version` =~ ^v12 ]] ; then npm run coverage ; npm install codeclimate-test-reporter ; codeclimate-test-reporter < coverage/lcov.info ; fi
